- name: be sure variables are deployed
  include_vars: "{{ ansible_os_family }}.yml"

- include: install/RedHat.yml
  when: ansible_os_family == 'RedHat'

- include: install/Debian.yml
  when: ansible_os_family == 'Debian'

- include: server.yml
  when: server

- include: client.yml
  when: client
