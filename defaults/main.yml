rabbitmq_users:
  - user: sensu
    password: "{{ vault_sensu_server_rabbitmq_users_sensu_password }}"
    vhost: /sensu
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    tags: sensu

rabbitmq_vhosts: [sensu] 

sensu_server_host: localhost
sensu_server_home: /etc/sensu
sensu_server_conf_d: "{{ sensu_home }}/conf.d"

sensu_server_apis:
  hello: world
  host: localhost

sensu_server_uchiwa_users:
  - name: helloadmin
    password: pw
    accessToken: ""
    readonly: false
  - name: helloguest
    password: pw2
    accessToken: ""
    readonly: true
